# Chanho Design System — Phase 1 Wave 3 (Plan B3) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 오버레이 컴포넌트 4종(Tooltip, Modal, Dropdown, Toast)과 Tabs를 추가해 Phase 1을 완결한다. 진입 준비로 shadow/blanket/inverse 토큰을 신설하고 B2 인계 항목(Select 그림자 임시변통 교체, SelectOption.disabled)을 해소한다.

**Architecture:** Wave 2 패턴 계승 — Radix primitive(Tooltip/Dialog/DropdownMenu/Tabs/Toast)를 평탄한 API로 래핑, data-state/data-highlighted 셀렉터 스타일링, Portal 콘텐츠는 :root의 CSS 변수를 그대로 상속(다크모드 자동). Toast는 Context 기반 `ToastProvider` + `useToast()` 훅으로 명령형 API를 제공한다.

**Tech Stack:** Wave 2 스택 그대로 (radix-ui 1.6.1, jsdom 폴리필 완비)

**참고:** 스펙 `2026-07-05-design-system-design.md`, B2 계획 `2026-07-06-phase1-wave2.md`, Radio 화살표키 사건(진행 원장·`Radio.test.tsx` 주석) — Radix roving-focus 키보드 테스트는 hold-tick-release 안무 필요
**후속:** Phase 1 완결. 이후는 스펙의 Phase 2(배포 체계) 또는 클론 앱 착수.

## Global Constraints

- 작업 루트: `C:\MSA_TEMPLATE\design-system\` (git 루트 `C:\MSA_TEMPLATE`, master 직접 커밋)
- 컴포넌트 CSS: **색상 리터럴만 금지**(hex·색상명·rgb()/hsl() 등 — Stylelint 강제, `transparent` 키워드도 회피할 것). 치수·위치(px, %, vw/vh, calc, inset, z-index)·시간 리터럴은 허용
- 시맨틱 토큰 light/dark 키 패리티 (기존 테스트 강제). 그림자·블랭킷의 rgba 값은 **토큰 값 문자열 안에만** 존재(TS 파일이라 Stylelint 대상 아님) — 컴포넌트 CSS는 `var(--chanho-*)`로만 소비
- 공통 테스트: 렌더/접근 가능 이름 + 소비자 className 병합은 전 컴포넌트 필수. ref 테스트는 합성 루트 컴포넌트(이번 웨이브 전부)에선 예외 — 테스트 파일 첫 줄 주석으로 사유 명시
- TDD: 테스트 → 실제 FAIL 관찰(RED) → 구현 → PASS(GREEN), 실출력 리포트 기록. 키보드 상호작용이 이유 없이 실패하면 Radio 사건처럼 Radix 소스를 열어 근본 원인부터 — 테스트 약화 금지
- Radix 경고 억제 목적의 콘솔 필터링 금지 — 경고가 나오면 원인을 고친다 (테스트 출력 pristine)
- index.ts export 알파벳 순서 유지, 컴포넌트별 배럴 파일 금지
- 커밋 컨벤션 `feat:` / `test:` / `chore:` / `docs:`
- 검증: `pnpm typecheck && pnpm test && pnpm lint:css`(각 태스크), `pnpm build-storybook`(Task 6)

---

### Task 1: 오버레이 준비 — 토큰 3종 + Select 인계 항목 해소

**Files:**
- Modify: `design-system/packages/tokens/src/semantic.ts` (shadow.overlay, color.blanket, color.background.inverse — light/dark)
- Modify: `design-system/packages/react/src/Select/Select.tsx` (SelectOption.disabled)
- Modify: `design-system/packages/react/src/Select/Select.module.css` (그림자 토큰 교체)
- Test: `design-system/packages/react/src/Select/Select.test.tsx` (disabled 옵션 테스트 추가)

**Interfaces:**
- Consumes: 기존 토큰 구조
- Produces: CSS 변수 6개 추가(양 테마 × 3키) — `--chanho-shadow-overlay`(오버레이 그림자), `--chanho-color-blanket`(모달 스크림), `--chanho-color-background-inverse`(툴팁 배경). Task 2~6이 소비. `SelectOption`에 `disabled?: boolean`.

- [ ] **Step 1: 시맨틱 토큰 추가 (light/dark 패리티)**

`design-system/packages/tokens/src/semantic.ts`의 `light` 객체에서 `"color.background.danger-subtle"` 뒤에 추가:

```ts
    "color.background.inverse": palette.gray[800],
    "color.blanket": "rgba(11, 14, 20, 0.55)",
```

`light` 객체의 `"color.border.danger"` 뒤(객체 마지막)에 추가:

```ts
    "shadow.overlay": "0 4px 12px rgba(22, 27, 38, 0.18)",
```

`dark` 객체에서 `"color.background.danger-subtle"` 뒤에 추가:

```ts
    "color.background.inverse": palette.gray[200],
    "color.blanket": "rgba(0, 0, 0, 0.65)",
```

`dark` 객체의 `"color.border.danger"` 뒤(객체 마지막)에 추가:

```ts
    "shadow.overlay": "0 4px 12px rgba(0, 0, 0, 0.55)",
```

Run: `pnpm --filter @chanho/tokens test && pnpm build:tokens && grep -c "chanho" C:/MSA_TEMPLATE/design-system/packages/tokens/dist/tokens.css`
Expected: 4/4 PASS, grep 85 (기존 79 + 6)

- [ ] **Step 2: Select disabled 실패 테스트 추가**

`design-system/packages/react/src/Select/Select.test.tsx`의 describe 블록 마지막에 추가:

```tsx
  it("disabled 옵션은 클릭해도 선택되지 않는다", async () => {
    const onValueChange = vi.fn();
    render(
      <Select
        label="우선순위"
        options={[
          { value: "high", label: "높음" },
          { value: "low", label: "낮음", disabled: true },
        ]}
        placeholder="선택"
        onValueChange={onValueChange}
      />,
    );
    await userEvent.click(screen.getByRole("combobox", { name: "우선순위" }));
    await userEvent.click(screen.getByRole("option", { name: "낮음" }));
    expect(onValueChange).not.toHaveBeenCalled();
  });
```

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — SelectOption에 disabled가 없어 TS 에러 또는 disabled 미전달로 onValueChange 호출됨. 실출력 기록 = RED.

- [ ] **Step 3: SelectOption.disabled 구현 + 그림자 토큰 교체**

`design-system/packages/react/src/Select/Select.tsx`의 `SelectOption`에 필드 추가:

```ts
export interface SelectOption {
  value: string;
  label: string;
  /** 선택 불가 옵션으로 표시한다. */
  disabled?: boolean;
}
```

같은 파일의 `RadixSelect.Item`에 `disabled={option.disabled}` prop 추가:

```tsx
                <RadixSelect.Item
                  key={option.value}
                  value={option.value}
                  className={styles.item}
                  disabled={option.disabled}
                >
```

`design-system/packages/react/src/Select/Select.module.css`의 `.content` 블록에서 box-shadow 줄을 교체:

```css
  box-shadow: var(--chanho-shadow-overlay);
```

(기존 `box-shadow: 0 4px 12px var(--chanho-color-background-neutral-hovered);` 줄 삭제)

- [ ] **Step 4: GREEN + 게이트**

Run: `pnpm --filter @chanho/react test && pnpm typecheck && pnpm lint:css`
Expected: 51/51 PASS (50 + 1), typecheck/lint 통과. 실출력 기록.

- [ ] **Step 5: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/tokens/src/semantic.ts design-system/packages/react/src/Select
git commit -m "feat(design-system): 오버레이 토큰(shadow/blanket/inverse) 신설 + Select disabled 옵션"
```

---

### Task 2: Tooltip 컴포넌트 (TDD, Radix)

**Files:**
- Create: `design-system/packages/react/src/Tooltip/Tooltip.test.tsx`
- Create: `design-system/packages/react/src/Tooltip/Tooltip.module.css`
- Create: `design-system/packages/react/src/Tooltip/Tooltip.tsx`
- Create: `design-system/packages/react/src/Tooltip/Tooltip.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `Tooltip` 네임스페이스, `--chanho-color-background-inverse`/`--chanho-color-text-inverse`/`--chanho-shadow-overlay`
- Produces: `Tooltip`, `TooltipProps` — `content: ReactNode`, `children: ReactElement`(트리거, 포커스 가능해야 함), `side?`(기본 top), `delayDuration?`(기본 300ms), `className?`(콘텐츠에 병합). ref 미노출(합성 루트).

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Tooltip/Tooltip.test.tsx`:

```tsx
// Tooltip은 Radix 합성 컴포넌트를 감싼 복합 루트라 ref를 노출하지 않는다 (공통 테스트 ref 예외).
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it } from "vitest";
import { Tooltip } from "./Tooltip";

function Subject(props: { className?: string }) {
  return (
    <Tooltip content="보관된 항목은 삭제할 수 없습니다" delayDuration={0} className={props.className}>
      <button type="button">삭제</button>
    </Tooltip>
  );
}

describe("Tooltip", () => {
  it("트리거에 호버하면 툴팁 내용이 표시된다", async () => {
    render(<Subject />);
    await userEvent.hover(screen.getByRole("button", { name: "삭제" }));
    const tooltip = await screen.findByRole("tooltip");
    expect(tooltip).toHaveTextContent("보관된 항목은 삭제할 수 없습니다");
  });

  it("Escape를 누르면 툴팁이 닫힌다", async () => {
    render(<Subject />);
    await userEvent.hover(screen.getByRole("button", { name: "삭제" }));
    await screen.findByRole("tooltip");
    await userEvent.keyboard("{Escape}");
    expect(screen.queryByRole("tooltip")).not.toBeInTheDocument();
  });

  it("툴팁이 열리면 트리거의 접근 가능 설명이 된다", async () => {
    render(<Subject />);
    const trigger = screen.getByRole("button", { name: "삭제" });
    await userEvent.hover(trigger);
    await screen.findByRole("tooltip");
    expect(trigger).toHaveAccessibleDescription("보관된 항목은 삭제할 수 없습니다");
  });

  it("소비자 className이 툴팁 콘텐츠에 병합된다", async () => {
    render(<Subject className="custom" />);
    await userEvent.hover(screen.getByRole("button", { name: "삭제" }));
    const tooltip = await screen.findByRole("tooltip");
    expect(tooltip).toHaveClass("custom");
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Tooltip` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Tooltip/Tooltip.module.css`:

```css
.content {
  max-width: 240px;
  padding: var(--chanho-space-50) var(--chanho-space-100);
  border-radius: var(--chanho-radius-small);
  background: var(--chanho-color-background-inverse);
  box-shadow: var(--chanho-shadow-overlay);
  color: var(--chanho-color-text-inverse);
  font-family: var(--chanho-font-family-sans);
  font-size: var(--chanho-font-size-100);
  line-height: var(--chanho-font-lineHeight-100);
  z-index: 50;
}
```

주의: lineHeight 토큰의 실제 CSS 변수명은 빌드 결과(`--chanho-font-lineHeight-100`)를 `dist/tokens.css`에서 확인하고 다르면 실제 이름으로 맞춘다(flatten이 camelCase 키를 그대로 쓰므로 대문자 H가 유지된다).

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Tooltip/Tooltip.tsx`:

```tsx
import type { ReactElement, ReactNode } from "react";
import { Tooltip as RadixTooltip } from "radix-ui";
import styles from "./Tooltip.module.css";

export interface TooltipProps {
  /** 툴팁 안에 표시할 내용. */
  content: ReactNode;
  /** 트리거 요소. 키보드 접근을 위해 포커스 가능해야 한다. */
  children: ReactElement;
  /**
   * 표시 위치.
   * @default 'top'
   */
  side?: "top" | "right" | "bottom" | "left";
  /**
   * 호버 후 표시까지의 지연(ms).
   * @default 300
   */
  delayDuration?: number;
  /** 툴팁 콘텐츠에 병합되는 클래스. */
  className?: string;
}

export function Tooltip({
  content,
  children,
  side = "top",
  delayDuration = 300,
  className,
}: TooltipProps) {
  return (
    <RadixTooltip.Provider delayDuration={delayDuration}>
      <RadixTooltip.Root>
        <RadixTooltip.Trigger asChild>{children}</RadixTooltip.Trigger>
        <RadixTooltip.Portal>
          <RadixTooltip.Content
            className={[styles.content, className].filter(Boolean).join(" ")}
            side={side}
            sideOffset={6}
          >
            {content}
          </RadixTooltip.Content>
        </RadixTooltip.Portal>
      </RadixTooltip.Root>
    </RadixTooltip.Provider>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 마지막(Toast는 아직 없음, TextField 뒤)에 추가:

```ts
export { Tooltip } from "./Tooltip/Tooltip";
export type { TooltipProps } from "./Tooltip/Tooltip";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 55/55 PASS (51 + Tooltip 4). 실출력 기록. 호버 기반 열림이 실패하면 Radix Tooltip 소스(포인터 타입 체크·:focus-visible 의존)를 열어 원인 규명 후 리포트 — 테스트 약화 금지.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Tooltip/Tooltip.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Button } from "../Button/Button";
import { Tooltip } from "./Tooltip";

const meta = {
  title: "Components/Tooltip",
  component: Tooltip,
} satisfies Meta<typeof Tooltip>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    content: "보관된 항목은 삭제할 수 없습니다",
    children: <Button variant="subtle">삭제</Button>,
  },
};

export const Bottom: Story = {
  args: {
    content: "아래쪽에 표시",
    side: "bottom",
    children: <Button variant="subtle">아래</Button>,
  },
};
```

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Tooltip design-system/packages/react/src/index.ts
git commit -m "feat(react): Tooltip 컴포넌트 - Radix 기반, inverse 배경"
```

---

### Task 3: Modal 컴포넌트 (TDD, Radix Dialog)

**Files:**
- Create: `design-system/packages/react/src/Modal/Modal.test.tsx`
- Create: `design-system/packages/react/src/Modal/Modal.module.css`
- Create: `design-system/packages/react/src/Modal/Modal.tsx`
- Create: `design-system/packages/react/src/Modal/Modal.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `Dialog` 네임스페이스, `--chanho-color-blanket`/`--chanho-shadow-overlay`
- Produces: `Modal`, `ModalProps` — `trigger: ReactNode`(클릭 시 열림), `title: string`(접근 가능 이름), `description?`, `children?`, `open?/onOpenChange?`(제어 지원), `className?`(콘텐츠 패널). 닫기 X 버튼(aria-label="닫기")·Escape·오버레이 클릭 닫힘은 Radix가 처리.

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Modal/Modal.test.tsx`:

```tsx
// Modal은 Radix Dialog 합성을 감싼 복합 루트라 ref를 노출하지 않는다 (공통 테스트 ref 예외).
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it } from "vitest";
import { Modal } from "./Modal";

function Subject(props: { className?: string; description?: string }) {
  return (
    <Modal
      trigger={<button type="button">열기</button>}
      title="이슈 삭제"
      description={props.description}
      className={props.className}
    >
      <p>본문 내용</p>
    </Modal>
  );
}

describe("Modal", () => {
  it("트리거를 클릭하면 제목을 접근 가능 이름으로 가진 dialog가 열린다", async () => {
    render(<Subject />);
    await userEvent.click(screen.getByRole("button", { name: "열기" }));
    expect(screen.getByRole("dialog", { name: "이슈 삭제" })).toBeInTheDocument();
    expect(screen.getByText("본문 내용")).toBeInTheDocument();
  });

  it("description이 dialog의 접근 가능 설명으로 연결된다", async () => {
    render(<Subject description="이 작업은 되돌릴 수 없습니다" />);
    await userEvent.click(screen.getByRole("button", { name: "열기" }));
    expect(screen.getByRole("dialog")).toHaveAccessibleDescription(
      "이 작업은 되돌릴 수 없습니다",
    );
  });

  it("닫기 버튼을 클릭하면 dialog가 닫힌다", async () => {
    render(<Subject />);
    await userEvent.click(screen.getByRole("button", { name: "열기" }));
    await userEvent.click(screen.getByRole("button", { name: "닫기" }));
    expect(screen.queryByRole("dialog")).not.toBeInTheDocument();
  });

  it("Escape를 누르면 dialog가 닫힌다", async () => {
    render(<Subject />);
    await userEvent.click(screen.getByRole("button", { name: "열기" }));
    await userEvent.keyboard("{Escape}");
    expect(screen.queryByRole("dialog")).not.toBeInTheDocument();
  });

  it("소비자 className이 콘텐츠 패널에 병합된다", async () => {
    render(<Subject className="custom" />);
    await userEvent.click(screen.getByRole("button", { name: "열기" }));
    expect(screen.getByRole("dialog")).toHaveClass("custom");
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Modal` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Modal/Modal.module.css`:

```css
.overlay {
  position: fixed;
  inset: 0;
  background: var(--chanho-color-blanket);
  z-index: 40;
}

.content {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: min(480px, calc(100vw - 32px));
  max-height: calc(100vh - 64px);
  overflow-y: auto;
  padding: var(--chanho-space-300);
  border-radius: var(--chanho-radius-large);
  background: var(--chanho-color-background-surface);
  box-shadow: var(--chanho-shadow-overlay);
  color: var(--chanho-color-text-default);
  font-family: var(--chanho-font-family-sans);
  z-index: 41;
}

.header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--chanho-space-100);
  margin-bottom: var(--chanho-space-100);
}

.title {
  margin: 0;
  font-size: var(--chanho-font-size-400);
  font-weight: var(--chanho-font-weight-semibold);
}

.close {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
  padding: 0;
  border: none;
  border-radius: var(--chanho-radius-small);
  background: none;
  color: var(--chanho-color-text-subtle);
  cursor: pointer;
}

.close:hover {
  background: var(--chanho-color-background-neutral);
}

.close:focus-visible {
  outline: 2px solid var(--chanho-color-border-focused);
  outline-offset: 2px;
}

.description {
  margin: 0 0 var(--chanho-space-200);
  color: var(--chanho-color-text-subtle);
  font-size: var(--chanho-font-size-200);
}
```

주의: `background: none`은 색상 리터럴이 아니므로 Stylelint 통과.

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Modal/Modal.tsx`:

```tsx
import type { ReactNode } from "react";
import { Dialog as RadixDialog } from "radix-ui";
import styles from "./Modal.module.css";

export interface ModalProps {
  /** 클릭 시 모달을 여는 트리거 요소. */
  trigger: ReactNode;
  /** 모달 제목. dialog의 접근 가능 이름이 된다. */
  title: string;
  /** 제목 아래 보조 설명. dialog의 접근 가능 설명이 된다. */
  description?: string;
  children?: ReactNode;
  /** 제어 모드 열림 상태. */
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  /** 콘텐츠 패널에 병합되는 클래스. */
  className?: string;
}

function XIcon() {
  return (
    <svg viewBox="0 0 12 12" width="12" height="12" fill="none" aria-hidden="true">
      <path
        d="M3 3l6 6M9 3l-6 6"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
    </svg>
  );
}

export function Modal({
  trigger,
  title,
  description,
  children,
  open,
  onOpenChange,
  className,
}: ModalProps) {
  return (
    <RadixDialog.Root open={open} onOpenChange={onOpenChange}>
      <RadixDialog.Trigger asChild>{trigger}</RadixDialog.Trigger>
      <RadixDialog.Portal>
        <RadixDialog.Overlay className={styles.overlay} />
        <RadixDialog.Content
          className={[styles.content, className].filter(Boolean).join(" ")}
          {...(description ? {} : { "aria-describedby": undefined })}
        >
          <div className={styles.header}>
            <RadixDialog.Title className={styles.title}>{title}</RadixDialog.Title>
            <RadixDialog.Close asChild>
              <button type="button" className={styles.close} aria-label="닫기">
                <XIcon />
              </button>
            </RadixDialog.Close>
          </div>
          {description ? (
            <RadixDialog.Description className={styles.description}>
              {description}
            </RadixDialog.Description>
          ) : null}
          {children}
        </RadixDialog.Content>
      </RadixDialog.Portal>
    </RadixDialog.Root>
  );
}
```

주의: description이 없을 때 `aria-describedby: undefined`를 명시적으로 넘겨 Radix의 "Missing Description" 콘솔 경고를 차단한다(테스트 출력 pristine 요건). 스프레드의 명시적 undefined가 Radix 내부 기본값을 덮어쓴다.

`design-system/packages/react/src/index.ts` — 알파벳 순서(Lozenge 뒤, Radio 앞)에 추가:

```ts
export { Modal } from "./Modal/Modal";
export type { ModalProps } from "./Modal/Modal";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 60/60 PASS (55 + Modal 5), 콘솔 경고 0. 실출력 기록.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Modal/Modal.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Button } from "../Button/Button";
import { Modal } from "./Modal";

const meta = {
  title: "Components/Modal",
  component: Modal,
} satisfies Meta<typeof Modal>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    trigger: <Button>이슈 삭제</Button>,
    title: "이슈 삭제",
    description: "이 작업은 되돌릴 수 없습니다.",
    children: (
      <div style={{ display: "flex", justifyContent: "flex-end", gap: 8 }}>
        <Button variant="subtle">취소</Button>
        <Button variant="danger">삭제</Button>
      </div>
    ),
  },
};

export const WithoutDescription: Story = {
  args: {
    trigger: <Button variant="subtle">설정</Button>,
    title: "설정",
    children: <p>설정 내용이 들어갑니다.</p>,
  },
};
```

주의: 스토리의 인라인 스타일은 docs 컨텍스트가 아니라 react 패키지 파일이지만 Stylelint는 .css만 검사하므로 통과 — 단, 기존 스토리 관례(args 중심)를 따르고 인라인 스타일은 레이아웃 데모 용도로만 최소 사용.

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Modal design-system/packages/react/src/index.ts
git commit -m "feat(react): Modal 컴포넌트 - Radix Dialog 기반, blanket/shadow 토큰"
```

---

### Task 4: Dropdown 컴포넌트 (TDD, Radix DropdownMenu)

**Files:**
- Create: `design-system/packages/react/src/Dropdown/Dropdown.test.tsx`
- Create: `design-system/packages/react/src/Dropdown/Dropdown.module.css`
- Create: `design-system/packages/react/src/Dropdown/Dropdown.tsx`
- Create: `design-system/packages/react/src/Dropdown/Dropdown.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `DropdownMenu` 네임스페이스, shadow.overlay 토큰
- Produces: `Dropdown`, `DropdownProps`, `DropdownItem` — `trigger: ReactNode`, `items: { label, onSelect?, danger?, disabled? }[]`, `className?`(콘텐츠). 키보드 내비·닫힘은 Radix 처리.

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Dropdown/Dropdown.test.tsx`:

```tsx
// Dropdown은 Radix 합성 컴포넌트를 감싼 복합 루트라 ref를 노출하지 않는다 (공통 테스트 ref 예외).
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { Dropdown } from "./Dropdown";

function Subject(props: {
  onEdit?: () => void;
  onDelete?: () => void;
  deleteDisabled?: boolean;
  className?: string;
}) {
  return (
    <Dropdown
      trigger={<button type="button">더보기</button>}
      className={props.className}
      items={[
        { label: "수정", onSelect: props.onEdit },
        { label: "삭제", onSelect: props.onDelete, danger: true, disabled: props.deleteDisabled },
      ]}
    />
  );
}

describe("Dropdown", () => {
  it("트리거를 클릭하면 메뉴와 항목들이 표시된다", async () => {
    render(<Subject />);
    await userEvent.click(screen.getByRole("button", { name: "더보기" }));
    expect(screen.getByRole("menu")).toBeInTheDocument();
    expect(screen.getByRole("menuitem", { name: "수정" })).toBeInTheDocument();
    expect(screen.getByRole("menuitem", { name: "삭제" })).toBeInTheDocument();
  });

  it("항목을 클릭하면 onSelect가 호출되고 메뉴가 닫힌다", async () => {
    const onEdit = vi.fn();
    render(<Subject onEdit={onEdit} />);
    await userEvent.click(screen.getByRole("button", { name: "더보기" }));
    await userEvent.click(screen.getByRole("menuitem", { name: "수정" }));
    expect(onEdit).toHaveBeenCalledTimes(1);
    expect(screen.queryByRole("menu")).not.toBeInTheDocument();
  });

  it("disabled 항목은 클릭해도 onSelect가 호출되지 않는다", async () => {
    const onDelete = vi.fn();
    render(<Subject onDelete={onDelete} deleteDisabled />);
    await userEvent.click(screen.getByRole("button", { name: "더보기" }));
    await userEvent.click(screen.getByRole("menuitem", { name: "삭제" }));
    expect(onDelete).not.toHaveBeenCalled();
  });

  it("Escape를 누르면 메뉴가 닫힌다", async () => {
    render(<Subject />);
    await userEvent.click(screen.getByRole("button", { name: "더보기" }));
    await userEvent.keyboard("{Escape}");
    expect(screen.queryByRole("menu")).not.toBeInTheDocument();
  });

  it("소비자 className이 메뉴 콘텐츠에 병합된다", async () => {
    render(<Subject className="custom" />);
    await userEvent.click(screen.getByRole("button", { name: "더보기" }));
    expect(screen.getByRole("menu")).toHaveClass("custom");
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Dropdown` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Dropdown/Dropdown.module.css`:

```css
.content {
  overflow: hidden;
  min-width: 160px;
  padding: var(--chanho-space-50);
  border: 1px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-medium);
  background: var(--chanho-color-background-surface);
  box-shadow: var(--chanho-shadow-overlay);
  font-family: var(--chanho-font-family-sans);
  z-index: 50;
}

.item {
  display: flex;
  align-items: center;
  padding: var(--chanho-space-100) var(--chanho-space-150);
  border-radius: var(--chanho-radius-small);
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  cursor: pointer;
  user-select: none;
  outline: none;
}

.item[data-highlighted] {
  background: var(--chanho-color-background-brand-subtle);
  color: var(--chanho-color-text-brand);
}

.item[data-disabled] {
  cursor: not-allowed;
  opacity: 0.5;
}

.danger {
  color: var(--chanho-color-text-danger);
}

.danger[data-highlighted] {
  background: var(--chanho-color-background-danger-subtle);
  color: var(--chanho-color-text-danger);
}
```

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Dropdown/Dropdown.tsx`:

```tsx
import type { ReactNode } from "react";
import { DropdownMenu as RadixDropdownMenu } from "radix-ui";
import styles from "./Dropdown.module.css";

export interface DropdownItem {
  label: string;
  /** 항목 선택 시 호출된다. */
  onSelect?: () => void;
  /** 파괴적 액션 표시(빨간 텍스트). */
  danger?: boolean;
  disabled?: boolean;
}

export interface DropdownProps {
  /** 클릭 시 메뉴를 여는 트리거 요소. */
  trigger: ReactNode;
  items: DropdownItem[];
  /** 메뉴 콘텐츠에 병합되는 클래스. */
  className?: string;
}

export function Dropdown({ trigger, items, className }: DropdownProps) {
  return (
    <RadixDropdownMenu.Root>
      <RadixDropdownMenu.Trigger asChild>{trigger}</RadixDropdownMenu.Trigger>
      <RadixDropdownMenu.Portal>
        <RadixDropdownMenu.Content
          className={[styles.content, className].filter(Boolean).join(" ")}
          sideOffset={4}
          align="start"
        >
          {items.map((item) => (
            <RadixDropdownMenu.Item
              key={item.label}
              className={[styles.item, item.danger ? styles.danger : null]
                .filter(Boolean)
                .join(" ")}
              disabled={item.disabled}
              onSelect={item.onSelect}
            >
              {item.label}
            </RadixDropdownMenu.Item>
          ))}
        </RadixDropdownMenu.Content>
      </RadixDropdownMenu.Portal>
    </RadixDropdownMenu.Root>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(Checkbox 뒤, Lozenge 앞)에 추가:

```ts
export { Dropdown } from "./Dropdown/Dropdown";
export type { DropdownItem, DropdownProps } from "./Dropdown/Dropdown";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 65/65 PASS (60 + Dropdown 5). 실출력 기록.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Dropdown/Dropdown.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Button } from "../Button/Button";
import { Dropdown } from "./Dropdown";

const meta = {
  title: "Components/Dropdown",
  component: Dropdown,
} satisfies Meta<typeof Dropdown>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    trigger: <Button variant="subtle">더보기</Button>,
    items: [
      { label: "수정" },
      { label: "복제" },
      { label: "보관", disabled: true },
      { label: "삭제", danger: true },
    ],
  },
};
```

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Dropdown design-system/packages/react/src/index.ts
git commit -m "feat(react): Dropdown 컴포넌트 - Radix DropdownMenu 기반, danger 항목"
```

---

### Task 5: Tabs 컴포넌트 (TDD, Radix)

**Files:**
- Create: `design-system/packages/react/src/Tabs/Tabs.test.tsx`
- Create: `design-system/packages/react/src/Tabs/Tabs.module.css`
- Create: `design-system/packages/react/src/Tabs/Tabs.tsx`
- Create: `design-system/packages/react/src/Tabs/Tabs.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `Tabs` 네임스페이스
- Produces: `Tabs`, `TabsProps`, `TabItem` — `label: string`(탭 목록 접근 이름), `items: { value, label, content }[]`, `defaultValue?`(기본: 첫 항목), `value?/onValueChange?`, `className?`(루트).

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Tabs/Tabs.test.tsx`:

```tsx
// Tabs는 Radix 합성 컴포넌트를 감싼 복합 루트라 ref를 노출하지 않는다 (공통 테스트 ref 예외).
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it } from "vitest";
import { Tabs } from "./Tabs";

const ITEMS = [
  { value: "overview", label: "개요", content: <p>개요 내용</p> },
  { value: "settings", label: "설정", content: <p>설정 내용</p> },
  { value: "activity", label: "활동", content: <p>활동 내용</p> },
];

describe("Tabs", () => {
  it("접근 가능 이름을 가진 tablist와 탭들이 렌더링된다", () => {
    render(<Tabs label="이슈 상세" items={ITEMS} />);
    expect(screen.getByRole("tablist", { name: "이슈 상세" })).toBeInTheDocument();
    expect(screen.getAllByRole("tab")).toHaveLength(3);
  });

  it("기본으로 첫 탭이 선택되고 해당 패널만 보인다", () => {
    render(<Tabs label="이슈 상세" items={ITEMS} />);
    expect(screen.getByRole("tab", { name: "개요" })).toHaveAttribute("aria-selected", "true");
    expect(screen.getByText("개요 내용")).toBeInTheDocument();
    expect(screen.queryByText("설정 내용")).not.toBeInTheDocument();
  });

  it("다른 탭을 클릭하면 패널이 전환된다", async () => {
    render(<Tabs label="이슈 상세" items={ITEMS} />);
    await userEvent.click(screen.getByRole("tab", { name: "설정" }));
    expect(screen.getByText("설정 내용")).toBeInTheDocument();
    expect(screen.queryByText("개요 내용")).not.toBeInTheDocument();
  });

  it("화살표 키로 탭을 이동할 수 있다", async () => {
    render(<Tabs label="이슈 상세" items={ITEMS} />);
    await userEvent.click(screen.getByRole("tab", { name: "개요" }));
    // Radix roving-focus는 포커스 이동을 setTimeout으로 미루므로 실사용자처럼
    // 키를 누른 채 한 틱 기다렸다 뗀다 (Radio.test.tsx의 동일 사건 참조).
    await userEvent.keyboard("{ArrowRight>}");
    await new Promise((resolve) => setTimeout(resolve, 0));
    await userEvent.keyboard("{/ArrowRight}");
    expect(screen.getByRole("tab", { name: "설정" })).toHaveAttribute("aria-selected", "true");
  });

  it("소비자 className이 루트에 병합된다", () => {
    const { container } = render(<Tabs label="이슈 상세" items={ITEMS} className="custom" />);
    expect(container.firstChild).toHaveClass("custom");
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Tabs` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Tabs/Tabs.module.css`:

```css
.root {
  font-family: var(--chanho-font-family-sans);
}

.list {
  display: flex;
  gap: var(--chanho-space-100);
  border-bottom: 2px solid var(--chanho-color-border-default);
}

.trigger {
  padding: var(--chanho-space-100) var(--chanho-space-150);
  border: none;
  background: none;
  color: var(--chanho-color-text-subtle);
  font-family: var(--chanho-font-family-sans);
  font-size: var(--chanho-font-size-200);
  font-weight: var(--chanho-font-weight-medium);
  cursor: pointer;
}

.trigger:hover {
  color: var(--chanho-color-text-default);
}

.trigger:focus-visible {
  outline: 2px solid var(--chanho-color-border-focused);
  outline-offset: -2px;
}

.trigger[data-state="active"] {
  color: var(--chanho-color-text-brand);
  box-shadow: inset 0 -2px 0 0 var(--chanho-color-background-brand);
}

.content {
  padding: var(--chanho-space-200) 0;
  color: var(--chanho-color-text-default);
}
```

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Tabs/Tabs.tsx`:

```tsx
import type { ReactNode } from "react";
import { Tabs as RadixTabs } from "radix-ui";
import styles from "./Tabs.module.css";

export interface TabItem {
  value: string;
  label: string;
  content: ReactNode;
}

export interface TabsProps {
  /** 탭 목록의 접근 가능 이름. */
  label: string;
  items: TabItem[];
  /** 비제어 초기 선택 탭. 생략하면 첫 항목. */
  defaultValue?: string;
  /** 제어 모드 선택 값. */
  value?: string;
  onValueChange?: (value: string) => void;
  /** 루트에 병합되는 클래스. */
  className?: string;
}

export function Tabs({ label, items, defaultValue, value, onValueChange, className }: TabsProps) {
  return (
    <RadixTabs.Root
      className={[styles.root, className].filter(Boolean).join(" ")}
      defaultValue={defaultValue ?? items[0]?.value}
      value={value}
      onValueChange={onValueChange}
    >
      <RadixTabs.List className={styles.list} aria-label={label}>
        {items.map((item) => (
          <RadixTabs.Trigger key={item.value} className={styles.trigger} value={item.value}>
            {item.label}
          </RadixTabs.Trigger>
        ))}
      </RadixTabs.List>
      {items.map((item) => (
        <RadixTabs.Content key={item.value} className={styles.content} value={item.value}>
          {item.content}
        </RadixTabs.Content>
      ))}
    </RadixTabs.Root>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(Switch 뒤, TextField 앞)에 추가:

```ts
export { Tabs } from "./Tabs/Tabs";
export type { TabItem, TabsProps } from "./Tabs/Tabs";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 70/70 PASS (65 + Tabs 5). 실출력 기록. 화살표 키 테스트가 hold-tick-release로도 실패하면 Radix Tabs의 activationMode 경로를 소스에서 확인 후 리포트.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Tabs/Tabs.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Tabs } from "./Tabs";

const meta = {
  title: "Components/Tabs",
  component: Tabs,
} satisfies Meta<typeof Tabs>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    label: "이슈 상세",
    items: [
      { value: "overview", label: "개요", content: <p>개요 내용입니다.</p> },
      { value: "settings", label: "설정", content: <p>설정 내용입니다.</p> },
      { value: "activity", label: "활동", content: <p>활동 내용입니다.</p> },
    ],
  },
};
```

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Tabs design-system/packages/react/src/index.ts
git commit -m "feat(react): Tabs 컴포넌트 - Radix 기반, 밑줄 인디케이터"
```

---

### Task 6: Toast 컴포넌트 (TDD, Radix) + Wave 3 최종 통합 검증

**Files:**
- Create: `design-system/packages/react/src/Toast/Toast.test.tsx`
- Create: `design-system/packages/react/src/Toast/Toast.module.css`
- Create: `design-system/packages/react/src/Toast/Toast.tsx`
- Create: `design-system/packages/react/src/Toast/Toast.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `Toast` 네임스페이스, shadow.overlay 토큰
- Produces: `ToastProvider`(앱 루트에 한 번 감쌈, `duration?` 기본 5000ms), `useToast()` 훅 — `toast({ title, description?, appearance? })` 반환, `ToastOptions` 타입. Provider 밖 사용 시 명확한 에러.

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Toast/Toast.test.tsx`:

```tsx
// ToastProvider/useToast는 Context 기반 합성이라 ref를 노출하지 않는다 (공통 테스트 ref 예외).
// className은 개별 토스트가 아닌 Provider 관할이라 className 병합 테스트도 해당 없음 — 대신
// appearance가 data-appearance 속성으로 노출되는지를 계약으로 검증한다.
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { ToastProvider, useToast } from "./Toast";

function Demo() {
  const toast = useToast();
  return (
    <button
      type="button"
      onClick={() =>
        toast({ title: "저장됨", description: "변경 사항이 저장되었습니다", appearance: "success" })
      }
    >
      알림 발생
    </button>
  );
}

function Broken() {
  useToast();
  return null;
}

describe("Toast", () => {
  it("ToastProvider 밖에서 useToast를 쓰면 명확한 에러를 던진다", () => {
    const spy = vi.spyOn(console, "error").mockImplementation(() => {});
    expect(() => render(<Broken />)).toThrow(/ToastProvider/);
    spy.mockRestore();
  });

  it("toast를 호출하면 제목·설명이 표시되고 appearance가 노출된다", async () => {
    render(
      <ToastProvider>
        <Demo />
      </ToastProvider>,
    );
    await userEvent.click(screen.getByRole("button", { name: "알림 발생" }));
    expect(screen.getByText("저장됨")).toBeInTheDocument();
    expect(screen.getByText("변경 사항이 저장되었습니다")).toBeInTheDocument();
    expect(screen.getByRole("status")).toHaveAttribute("data-appearance", "success");
  });

  it("닫기 버튼을 클릭하면 토스트가 사라진다", async () => {
    render(
      <ToastProvider>
        <Demo />
      </ToastProvider>,
    );
    await userEvent.click(screen.getByRole("button", { name: "알림 발생" }));
    await userEvent.click(screen.getByRole("button", { name: "닫기" }));
    await waitFor(() => expect(screen.queryByText("저장됨")).not.toBeInTheDocument());
  });

  it("여러 번 호출하면 토스트가 쌓인다", async () => {
    render(
      <ToastProvider>
        <Demo />
      </ToastProvider>,
    );
    await userEvent.click(screen.getByRole("button", { name: "알림 발생" }));
    await userEvent.click(screen.getByRole("button", { name: "알림 발생" }));
    expect(screen.getAllByText("저장됨")).toHaveLength(2);
  });
});
```

주의: Radix Toast Root의 기본 role은 type에 따라 status/alert다 — `getByRole("status")`가 실패하면 실제 렌더된 role을 확인해 테스트가 아닌 **컴포넌트의 type prop**을 조정해 role="status"가 되게 한다(배경 알림 시맨틱).

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Toast` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Toast/Toast.module.css`:

```css
.viewport {
  position: fixed;
  right: var(--chanho-space-200);
  bottom: var(--chanho-space-200);
  display: flex;
  flex-direction: column;
  gap: var(--chanho-space-100);
  width: 320px;
  max-width: calc(100vw - 32px);
  margin: 0;
  padding: 0;
  list-style: none;
  z-index: 60;
}

.toast {
  display: flex;
  align-items: flex-start;
  gap: var(--chanho-space-100);
  padding: var(--chanho-space-150) var(--chanho-space-200);
  border: 1px solid var(--chanho-color-border-default);
  border-left-width: 4px;
  border-radius: var(--chanho-radius-medium);
  background: var(--chanho-color-background-surface);
  box-shadow: var(--chanho-shadow-overlay);
  font-family: var(--chanho-font-family-sans);
}

.toast[data-appearance="info"] {
  border-left-color: var(--chanho-color-background-brand);
}

.toast[data-appearance="success"] {
  border-left-color: var(--chanho-color-text-success);
}

.toast[data-appearance="danger"] {
  border-left-color: var(--chanho-color-background-danger);
}

.body {
  flex: 1;
  min-width: 0;
}

.title {
  margin: 0;
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  font-weight: var(--chanho-font-weight-semibold);
}

.description {
  margin: var(--chanho-space-25) 0 0;
  color: var(--chanho-color-text-subtle);
  font-size: var(--chanho-font-size-100);
}

.close {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 20px;
  height: 20px;
  padding: 0;
  border: none;
  border-radius: var(--chanho-radius-small);
  background: none;
  color: var(--chanho-color-text-subtle);
  cursor: pointer;
}

.close:hover {
  background: var(--chanho-color-background-neutral);
}

.close:focus-visible {
  outline: 2px solid var(--chanho-color-border-focused);
  outline-offset: 2px;
}
```

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Toast/Toast.tsx`:

```tsx
import { createContext, useCallback, useContext, useRef, useState } from "react";
import type { ReactNode } from "react";
import { Toast as RadixToast } from "radix-ui";
import styles from "./Toast.module.css";

export interface ToastOptions {
  title: string;
  description?: string;
  /**
   * 시각적 의미. 좌측 색 띠로 표시된다.
   * @default 'info'
   */
  appearance?: "info" | "success" | "danger";
}

type ToastEntry = ToastOptions & { id: number };

const ToastContext = createContext<((options: ToastOptions) => void) | null>(null);

/** toast({ title, description?, appearance? })를 반환한다. ToastProvider 안에서만 사용 가능. */
export function useToast() {
  const toast = useContext(ToastContext);
  if (!toast) {
    throw new Error("useToast는 ToastProvider 안에서만 사용할 수 있다");
  }
  return toast;
}

function XIcon() {
  return (
    <svg viewBox="0 0 12 12" width="12" height="12" fill="none" aria-hidden="true">
      <path
        d="M3 3l6 6M9 3l-6 6"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
    </svg>
  );
}

export interface ToastProviderProps {
  children: ReactNode;
  /**
   * 자동 사라짐까지의 시간(ms).
   * @default 5000
   */
  duration?: number;
}

/** 앱 루트에 한 번 감싼다. 토스트는 우하단에 쌓인다. */
export function ToastProvider({ children, duration = 5000 }: ToastProviderProps) {
  const [toasts, setToasts] = useState<ToastEntry[]>([]);
  const counter = useRef(0);

  const toast = useCallback((options: ToastOptions) => {
    counter.current += 1;
    setToasts((prev) => [...prev, { ...options, id: counter.current }]);
  }, []);

  const remove = useCallback((id: number) => {
    setToasts((prev) => prev.filter((entry) => entry.id !== id));
  }, []);

  return (
    <ToastContext.Provider value={toast}>
      <RadixToast.Provider swipeDirection="right" duration={duration}>
        {children}
        {toasts.map((entry) => (
          <RadixToast.Root
            key={entry.id}
            type="background"
            className={styles.toast}
            data-appearance={entry.appearance ?? "info"}
            onOpenChange={(open) => {
              if (!open) remove(entry.id);
            }}
          >
            <div className={styles.body}>
              <RadixToast.Title className={styles.title}>{entry.title}</RadixToast.Title>
              {entry.description ? (
                <RadixToast.Description className={styles.description}>
                  {entry.description}
                </RadixToast.Description>
              ) : null}
            </div>
            <RadixToast.Close asChild>
              <button type="button" className={styles.close} aria-label="닫기">
                <XIcon />
              </button>
            </RadixToast.Close>
          </RadixToast.Root>
        ))}
        <RadixToast.Viewport className={styles.viewport} label="알림" />
      </RadixToast.Provider>
    </ToastContext.Provider>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(TextField 뒤, Tooltip 앞)에 추가:

```ts
export { ToastProvider, useToast } from "./Toast/Toast";
export type { ToastOptions, ToastProviderProps } from "./Toast/Toast";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 74/74 PASS (70 + Toast 4). 실출력 기록.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Toast/Toast.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Button } from "../Button/Button";
import { ToastProvider, useToast } from "./Toast";

function Demo() {
  const toast = useToast();
  return (
    <div style={{ display: "flex", gap: 8 }}>
      <Button
        variant="subtle"
        onClick={() => toast({ title: "저장됨", description: "변경 사항이 저장되었습니다", appearance: "success" })}
      >
        성공 토스트
      </Button>
      <Button
        variant="subtle"
        onClick={() => toast({ title: "저장 실패", description: "네트워크를 확인하세요", appearance: "danger" })}
      >
        실패 토스트
      </Button>
      <Button variant="subtle" onClick={() => toast({ title: "새 댓글이 달렸습니다" })}>
        기본 토스트
      </Button>
    </div>
  );
}

const meta = {
  title: "Components/Toast",
  component: ToastProvider,
} satisfies Meta<typeof ToastProvider>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  render: () => (
    <ToastProvider>
      <Demo />
    </ToastProvider>
  ),
};
```

- [ ] **Step 7: Wave 3 최종 통합 검증**

Run: `cd C:/MSA_TEMPLATE/design-system && pnpm typecheck && pnpm test && pnpm lint:css && pnpm build-storybook`
Expected: typecheck 3패키지 / 78/78 (tokens 4 + react 74) / lint 통과 / Storybook 빌드 성공 — storybook-static/index.json에 Tooltip/Modal/Dropdown/Tabs/Toast 포함(총 15개 컴포넌트 스토리)

- [ ] **Step 8: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Toast design-system/packages/react/src/index.ts
git commit -m "feat(react): Toast 컴포넌트 - ToastProvider/useToast, appearance 3종"
```

---

## 완료 기준 (Plan B3 전체 = Phase 1 완결)

- `pnpm typecheck` — 3패키지 에러 0
- `pnpm test` — 78/78 (tokens 4 + react 74)
- `pnpm lint:css` — 통과
- `pnpm build-storybook` — 성공, Components에 15개 컴포넌트 스토리
- 수동(브라우저): Modal 블랭킷·Toast 쌓임·Tooltip inverse 배경이 다크모드에서 올바른지, 오버레이 키보드 조작(Escape/Tab 트랩), a11y 애드온 위반 0건
- Phase 1 종료. 다음 선택지: Phase 2(배포 체계 — npm 배포·Changesets·번들링) 또는 클론 앱(PMS) 착수. 미해결 인계: aria 병합 유틸(TextField 계열), 아이콘 패키지(Phase 4), axe CI화(Phase 2)
