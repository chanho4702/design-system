# W0: TextArea 컴포넌트 + 0.2.0 tarball Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ALM Front(지라 클론) 선행 작업 — TextField 패턴을 미러한 TextArea 컴포넌트를 @chanho/react에 추가하고 0.2.0 tarball을 재생성한다.

**Architecture:** 컴포넌트당 1폴더 컨벤션(`packages/react/src/TextArea/` 4파일). TextField와 동일한 label/description/error 접근성 배선, `ComponentPropsWithRef<"textarea">`(React 19 — forwardRef 없음). 배포는 Changesets minor → 0.2.0 → `pnpm pack`으로 artifacts/ tarball.

**Tech Stack:** React 19, CSS Modules(--chanho-* 토큰만), Vitest+RTL, Storybook 9, Changesets, pnpm.

## Global Constraints

- repo: `C:\MSA_TEMPLATE\design-system` (github.com/chanho4702/design-system, main 직접 커밋)
- 색상은 `var(--chanho-color-*)` 토큰만 — Stylelint가 hex/색상명/색함수 전부 차단
- 배럴(src/index.ts)은 알파벳 순: Tabs와 TextField 사이에 삽입
- 게이트: 루트 `pnpm test` + `pnpm typecheck` + `pnpm lint:css`
- TDD: RED를 실제로 관찰한 후 구현 (원장 컨벤션 — 증거 조작 금지)

---

### Task 1: TextArea 컴포넌트 (TDD)

**Files:**
- Create: `packages/react/src/TextArea/TextArea.test.tsx`
- Create: `packages/react/src/TextArea/TextArea.tsx`
- Create: `packages/react/src/TextArea/TextArea.module.css`
- Create: `packages/react/src/TextArea/TextArea.stories.tsx`
- Modify: `packages/react/src/index.ts` (Tabs export와 TextField export 사이)

**Interfaces:**
- Consumes: `@chanho/tokens` CSS 변수(--chanho-space-50/100, font-family-sans, font-size-100/200, font-weight-medium, radius-medium, color-text-default/subtle/danger, color-border-default/focused/danger, color-background-surface/subtle) — 전부 기존 존재(TextField가 사용 중)
- Produces: `export function TextArea(props: TextAreaProps)`, `export interface TextAreaProps extends ComponentPropsWithRef<"textarea"> { label: string; description?: string; error?: string }` — W1 이후 ALM Front가 `import { TextArea } from "@chanho/react"`로 소비

- [ ] **Step 1: 실패하는 테스트 작성**

`packages/react/src/TextArea/TextArea.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { createRef } from "react";
import { describe, expect, it } from "vitest";
import { TextArea } from "./TextArea";

describe("TextArea", () => {
  it("라벨이 textarea와 연결된다", () => {
    render(<TextArea label="설명" />);
    expect(screen.getByLabelText("설명")).toBeInTheDocument();
  });

  it("타이핑한 값이 입력된다", async () => {
    const user = userEvent.setup();
    render(<TextArea label="설명" />);
    const textarea = screen.getByLabelText("설명");
    await user.type(textarea, "여러 줄\n입력");
    expect(textarea).toHaveValue("여러 줄\n입력");
  });

  it("description이 접근 가능한 설명으로 연결된다", () => {
    render(<TextArea label="설명" description="마크다운은 지원하지 않습니다" />);
    expect(screen.getByLabelText("설명")).toHaveAccessibleDescription(
      "마크다운은 지원하지 않습니다",
    );
  });

  it("error 지정 시 aria-invalid와 에러 메시지가 연결된다", () => {
    render(<TextArea label="설명" error="필수 항목입니다" />);
    const textarea = screen.getByLabelText("설명");
    expect(textarea).toHaveAttribute("aria-invalid", "true");
    expect(textarea).toHaveAccessibleDescription("필수 항목입니다");
  });

  it("className이 루트 래퍼에 병합된다", () => {
    const { container } = render(<TextArea label="설명" className="custom" />);
    expect(container.firstChild).toHaveClass("custom");
  });

  it("ref로 textarea 요소에 접근할 수 있다", () => {
    const ref = createRef<HTMLTextAreaElement>();
    render(<TextArea label="설명" ref={ref} />);
    expect(ref.current).toBeInstanceOf(HTMLTextAreaElement);
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `cd C:/MSA_TEMPLATE/design-system/packages/react && pnpm vitest run src/TextArea`
Expected: FAIL — `Failed to resolve import "./TextArea"` (모듈 없음)

- [ ] **Step 3: 최소 구현**

`packages/react/src/TextArea/TextArea.tsx`:

```tsx
import { useId } from "react";
import type { ComponentPropsWithRef } from "react";
import styles from "./TextArea.module.css";

export interface TextAreaProps extends ComponentPropsWithRef<"textarea"> {
  label: string;
  description?: string;
  error?: string;
}

export function TextArea({ label, description, error, id, className, ...rest }: TextAreaProps) {
  const autoId = useId();
  const textareaId = id ?? autoId;
  const descId = `${textareaId}-desc`;
  const errorId = `${textareaId}-error`;
  const describedBy =
    [description ? descId : null, error ? errorId : null].filter(Boolean).join(" ") || undefined;

  return (
    <div className={[styles.field, className].filter(Boolean).join(" ")}>
      <label className={styles.label} htmlFor={textareaId}>
        {label}
      </label>
      <textarea
        id={textareaId}
        className={[styles.textarea, error ? styles.invalid : null].filter(Boolean).join(" ")}
        aria-invalid={error ? true : undefined}
        aria-describedby={describedBy}
        {...rest}
      />
      {description ? (
        <p id={descId} className={styles.description}>
          {description}
        </p>
      ) : null}
      {error ? (
        <p id={errorId} className={styles.error}>
          {error}
        </p>
      ) : null}
    </div>
  );
}
```

`packages/react/src/TextArea/TextArea.module.css` — TextField.module.css를 미러하되 `.input`→`.textarea`(height 40px 대신 min-height 96px + resize: vertical + line-height):

```css
.field {
  display: flex;
  flex-direction: column;
  gap: var(--chanho-space-50);
  font-family: var(--chanho-font-family-sans);
}

.label {
  font-size: var(--chanho-font-size-100);
  font-weight: var(--chanho-font-weight-medium);
  color: var(--chanho-color-text-default);
}

.textarea {
  min-height: 96px;
  resize: vertical;
  padding: var(--chanho-space-100);
  font-family: var(--chanho-font-family-sans);
  font-size: var(--chanho-font-size-200);
  line-height: 1.5;
  color: var(--chanho-color-text-default);
  background: var(--chanho-color-background-surface);
  border: 2px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-medium);
  transition: border-color 120ms ease;
}

.textarea:hover:not(:disabled) {
  background: var(--chanho-color-background-subtle);
}

.textarea:focus-visible {
  outline: none;
  border-color: var(--chanho-color-border-focused);
}

.textarea:disabled {
  opacity: 0.5;
  resize: none;
}

.invalid {
  border-color: var(--chanho-color-border-danger);
}

.description {
  margin: 0;
  font-size: var(--chanho-font-size-100);
  color: var(--chanho-color-text-subtle);
}

.error {
  margin: 0;
  font-size: var(--chanho-font-size-100);
  color: var(--chanho-color-text-danger);
}
```

주의: 위 CSS의 `.field/.label/.description/.error`는 실제 TextField.module.css의 해당 블록과 **속성·값을 그대로 일치**시킬 것(구현 시 TextField.module.css를 열어 대조 — 위 코드가 원본과 다르면 원본을 따른다).

`packages/react/src/index.ts` — Tabs와 TextField 사이에 삽입:

```ts
export { TextArea } from "./TextArea/TextArea";
export type { TextAreaProps } from "./TextArea/TextArea";
```

- [ ] **Step 4: GREEN 확인**

Run: `cd C:/MSA_TEMPLATE/design-system/packages/react && pnpm vitest run src/TextArea`
Expected: PASS 6/6

- [ ] **Step 5: 스토리 작성**

`packages/react/src/TextArea/TextArea.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { TextArea } from "./TextArea";

const meta = {
  title: "Components/TextArea",
  component: TextArea,
  args: { label: "설명" },
} satisfies Meta<typeof TextArea>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
export const WithDescription: Story = {
  args: { description: "이슈의 배경과 재현 방법을 적어주세요." },
};
export const WithError: Story = { args: { error: "필수 항목입니다" } };
export const Disabled: Story = { args: { disabled: true } };
```

- [ ] **Step 6: 전체 게이트**

Run: `cd C:/MSA_TEMPLATE/design-system && pnpm test && pnpm typecheck && pnpm lint:css`
Expected: 전체 PASS (react 75→81, tokens 4), typecheck 3패키지 0에러, stylelint 통과

- [ ] **Step 7: 커밋**

```bash
cd C:/MSA_TEMPLATE/design-system
git add packages/react/src/TextArea packages/react/src/index.ts docs/superpowers/plans/2026-07-10-w0-textarea.md
git commit -m "feat(react): TextArea 컴포넌트 — TextField 패턴 미러 (ALM Front W0 선행)"
```

---

### Task 2: 0.2.0 버전업 + tarball 재생성 + 소비자 검증

**Files:**
- Create: `.changeset/w0-textarea.md`
- Modify: `packages/react/package.json`(version — changeset이 자동), `packages/react/CHANGELOG.md`(자동)
- Modify: `examples/consumer/package.json`(react tarball 경로 0.2.0), `examples/consumer/src/App.tsx`(TextArea 스모크 추가)
- Create: `artifacts/chanho-react-0.2.0.tgz` (바이너리 — git 추적 여부는 기존 artifacts/*.tgz와 동일하게)

**Interfaces:**
- Consumes: Task 1의 TextArea export
- Produces: `artifacts/chanho-react-0.2.0.tgz` — ALM Front W1이 `file:../design-system/artifacts/chanho-react-0.2.0.tgz`로 설치. tokens는 0.1.0 유지(`chanho-tokens-0.1.0.tgz`)

- [ ] **Step 1: changeset 작성**

`.changeset/w0-textarea.md`:

```md
---
"@chanho/react": minor
---

TextArea 컴포넌트 추가 — label/description/error 접근성 배선, TextField 패턴 미러 (ALM Front 선행 작업)
```

- [ ] **Step 2: 버전업 실행**

Run: `cd C:/MSA_TEMPLATE/design-system && pnpm version-packages`
Expected: `packages/react/package.json` version 0.1.0→0.2.0, CHANGELOG.md 항목 추가, changeset 파일 소멸. tokens는 무변경(0.1.0).

- [ ] **Step 3: 빌드 + tarball 생성**

Run:
```bash
cd C:/MSA_TEMPLATE/design-system && pnpm -r build
cd packages/react && pnpm pack --pack-destination ../../artifacts
```
Expected: `artifacts/chanho-react-0.2.0.tgz` 생성. `tar -tzf`로 dist/index.js·dist/index.d.ts·dist/styles.css 포함 확인, styles.css 안에 TextArea 클래스 존재 확인(`tar -xzOf ... package/dist/styles.css | grep -i textarea`).

- [ ] **Step 4: 소비자 검증 갱신**

`examples/consumer/package.json`의 `"@chanho/react"`를 `file:../../artifacts/chanho-react-0.2.0.tgz`로 변경.
`examples/consumer/src/App.tsx`의 import에 `TextArea` 추가 + JSX 아무 섹션에 `<TextArea label="TextArea 스모크" description="0.2.0 tarball 검증" />` 1개 추가.

Run: `cd C:/MSA_TEMPLATE/design-system/examples/consumer && pnpm install && pnpm typecheck && pnpm build`
Expected: 설치·타입·빌드 전부 그린 (TextArea 타입이 tarball d.ts에서 해석됨)

- [ ] **Step 5: 개발 게이트 불변 확인**

Run: `cd C:/MSA_TEMPLATE/design-system && pnpm test && pnpm typecheck`
Expected: 전부 PASS (배포 빌드가 개발 게이트를 깨지 않음)

- [ ] **Step 6: 커밋 + 푸시**

```bash
cd C:/MSA_TEMPLATE/design-system
git add -A
git commit -m "chore(release): @chanho/react 0.2.0 — TextArea, tarball 재생성 + 소비자 검증"
git push
```

---

## 검증 요약

1. `pnpm test` — react 81(기존 75+신규 6)/tokens 4 전부 그린
2. `pnpm typecheck` 3패키지 + `pnpm lint:css`(TextArea.module.css 토큰-온리 검증)
3. examples/consumer: 0.2.0 tarball 설치 + tsc + vite build 그린
4. 원장 컨벤션대로 RED 실관찰 증거를 커밋 메시지가 아닌 리포트에 남김
