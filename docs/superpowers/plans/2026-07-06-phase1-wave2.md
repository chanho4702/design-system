# Chanho Design System — Phase 1 Wave 2 (Plan B2) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Radix UI(headless primitives)를 도입하고 폼 컴포넌트 5종 — TextField, Checkbox, Radio(Group), Switch, Select — 을 추가한다. 진입 전 인프라 정리로 docs 앱 vite를 ^7로 정렬해 lockfile 이중 트리를 해소한다.

**Architecture:** Wave 1과 동일한 4파일 패턴. 동작 계층은 `radix-ui` 단일 패키지의 네임스페이스 primitive(Checkbox/RadioGroup/Switch/Select)를 사용하고, TextField는 네이티브 input + 접근성 배선(useId·aria-describedby)으로 직접 구현한다. Radix 상태 스타일링은 `[data-state="..."]` 셀렉터로 한다. jsdom에 없는 API(ResizeObserver, pointer capture, scrollIntoView)는 vitest.setup.ts에서 폴리필한다(Select 태스크).

**Tech Stack:** Wave 1 스택 + `radix-ui` ^1.4 (버전 해석이 다르면 최신 1.x로 최소 적응 후 리포트에 공개), docs vite ^7

**참고:** 스펙 `2026-07-05-design-system-design.md`, B1 계획 `2026-07-06-phase1-wave1.md`
**후속:** Plan B3(오버레이: Modal/Dropdown/Tooltip/Toast + Tabs)는 이 계획 완료 후 작성.

## Global Constraints

- 작업 루트: `C:\MSA_TEMPLATE\design-system\` (git 루트 `C:\MSA_TEMPLATE`, master 직접 커밋)
- 컴포넌트 CSS는 `--chanho-*` 변수만. 허용 리터럴: 컴포넌트 고유 치수(px), `line-height: 1`, 애니메이션/전환 시간. 색상 리터럴 금지(Stylelint 강제)
- 시맨틱 컬러 토큰 light/dark 키 패리티 (기존 테스트 강제)
- **공통 테스트 3종 필수**(B1 최종 리뷰 반영): 모든 신규 컴포넌트 테스트에 ① 렌더/접근 가능 이름 ② 소비자 className 병합 ③ ref로 DOM 노드 접근(합성 루트라 ref 노출이 없는 Select는 예외 — 사유를 테스트 파일 주석으로 명시)
- TDD: 테스트 작성 → 실제 FAIL 관찰(RED) → 구현 → PASS(GREEN), 실출력 리포트 기록
- 폼 컨트롤은 라벨 필수 연결 — 접근 가능 이름 없는 컨트롤 금지 (테스트로 강제)
- 각 컴포넌트 `src/index.ts` export 추가는 **알파벳 순서 유지**
- 커밋 컨벤션 `feat:` / `test:` / `chore:` / `docs:`
- 검증: `pnpm typecheck && pnpm test && pnpm lint:css`(각 태스크), `pnpm build-storybook`(Task 1과 Task 6)

---

### Task 1: 인프라 — docs vite ^7 정렬 + radix-ui 도입

**Files:**
- Modify: `design-system/apps/docs/package.json` (vite ^6.0.0 → ^7.0.0)
- Modify: `design-system/packages/react/package.json` (dependencies에 radix-ui 추가)

**Interfaces:**
- Consumes: B1까지의 워크스페이스
- Produces: lockfile의 vite 메이저 단일화(7.x), `packages/react`에서 `import { Checkbox, RadioGroup, Switch, Select } from "radix-ui"` 사용 가능. Task 3~6이 소비.

- [ ] **Step 1: 버전 변경**

`design-system/apps/docs/package.json` devDependencies에서:

```json
    "vite": "^7.0.0"
```

`design-system/packages/react/package.json` dependencies에 추가(@chanho/tokens 다음):

```json
    "radix-ui": "^1.4.0"
```

- [ ] **Step 2: 설치 및 이중 트리 해소 확인**

Run: `cd C:/MSA_TEMPLATE/design-system && pnpm install`
Expected: 에러 없음. 빌드 스크립트 차단(ERR_PNPM_IGNORED_BUILDS) 발생 시 필요한 패키지만 allowBuilds에 추가하고 리포트·커밋 메시지에 공개.

Run: `grep -c "vite@6" C:/MSA_TEMPLATE/design-system/pnpm-lock.yaml || echo 0`
Expected: 0 (vite 6 트리 소멸 — 남아 있으면 원인 조사 후 리포트)

- [ ] **Step 3: 전체 게이트 확인**

Run: `pnpm typecheck && pnpm test && pnpm lint:css && pnpm build-storybook`
Expected: typecheck 3패키지 / 25/25 / lint 통과 / Storybook vite 7로 빌드 성공(5컴포넌트 스토리 유지)

- [ ] **Step 4: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/apps/docs/package.json design-system/packages/react/package.json design-system/pnpm-lock.yaml
git commit -m "chore(design-system): docs vite ^7 정렬 + radix-ui 도입"
```

---

### Task 2: TextField 컴포넌트 (TDD, Radix 불필요)

**Files:**
- Modify: `design-system/packages/tokens/src/semantic.ts` (color.border.danger 추가)
- Create: `design-system/packages/react/src/TextField/TextField.test.tsx`
- Create: `design-system/packages/react/src/TextField/TextField.module.css`
- Create: `design-system/packages/react/src/TextField/TextField.tsx`
- Create: `design-system/packages/react/src/TextField/TextField.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: 기존 토큰 + 신규 `--chanho-color-border-danger`
- Produces: `TextField`, `TextFieldProps` — `label: string`(필수), `description?`, `error?`(있으면 aria-invalid + 에러 스타일), 나머지 네이티브 input props(ref는 input에 전달, className은 루트 래퍼에 적용 — JSDoc에 명시)

- [ ] **Step 1: 토큰 추가 (light/dark 패리티)**

`design-system/packages/tokens/src/semantic.ts`의 `light` 객체 `"color.border.focused"` 뒤에:

```ts
    "color.border.danger": palette.red[600],
```

`dark` 객체 `"color.border.focused"` 뒤에:

```ts
    "color.border.danger": palette.red[500],
```

Run: `pnpm --filter @chanho/tokens test && pnpm build:tokens`
Expected: 4/4 PASS, 재빌드 성공 (CSS 변수 79개)

- [ ] **Step 2: 실패 테스트 작성**

`design-system/packages/react/src/TextField/TextField.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { createRef } from "react";
import { describe, expect, it } from "vitest";
import { TextField } from "./TextField";

describe("TextField", () => {
  it("라벨이 input과 연결되어 라벨 텍스트로 찾을 수 있다", () => {
    render(<TextField label="이메일" />);
    expect(screen.getByLabelText("이메일")).toBeInTheDocument();
  });

  it("타이핑한 값이 입력된다", async () => {
    render(<TextField label="이메일" />);
    const input = screen.getByLabelText("이메일");
    await userEvent.type(input, "a@b.c");
    expect(input).toHaveValue("a@b.c");
  });

  it("description이 접근 가능 설명으로 연결된다", () => {
    render(<TextField label="이메일" description="회사 이메일을 입력하세요" />);
    expect(screen.getByLabelText("이메일")).toHaveAccessibleDescription(
      "회사 이메일을 입력하세요",
    );
  });

  it("error가 있으면 aria-invalid가 되고 에러 메시지가 설명으로 연결된다", () => {
    render(<TextField label="이메일" error="필수 항목입니다" />);
    const input = screen.getByLabelText("이메일");
    expect(input).toHaveAttribute("aria-invalid", "true");
    expect(input).toHaveAccessibleDescription("필수 항목입니다");
  });

  it("소비자 className이 루트 래퍼에 병합된다", () => {
    const { container } = render(<TextField label="이메일" className="custom" />);
    expect(container.firstChild).toHaveClass("custom");
  });

  it("ref로 input DOM 노드에 접근할 수 있다", () => {
    const ref = createRef<HTMLInputElement>();
    render(<TextField label="이메일" ref={ref} />);
    expect(ref.current).toBeInstanceOf(HTMLInputElement);
  });
});
```

- [ ] **Step 3: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./TextField` 모듈 없음. 실출력 기록.

- [ ] **Step 4: 스타일 작성**

`design-system/packages/react/src/TextField/TextField.module.css`:

```css
.field {
  display: flex;
  flex-direction: column;
  gap: var(--chanho-space-50);
  font-family: var(--chanho-font-family-sans);
}

.label {
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  font-weight: var(--chanho-font-weight-medium);
}

.input {
  height: 40px;
  padding: 0 var(--chanho-space-100);
  border: 2px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-medium);
  background: var(--chanho-color-background-surface);
  color: var(--chanho-color-text-default);
  font-family: var(--chanho-font-family-sans);
  font-size: var(--chanho-font-size-200);
  transition: border-color 120ms ease;
}

.input:hover:not(:disabled) {
  background: var(--chanho-color-background-subtle);
}

.input:focus-visible {
  outline: none;
  border-color: var(--chanho-color-border-focused);
}

.input:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.invalid {
  border-color: var(--chanho-color-border-danger);
}

.description {
  margin: 0;
  color: var(--chanho-color-text-subtle);
  font-size: var(--chanho-font-size-100);
}

.error {
  margin: 0;
  color: var(--chanho-color-text-danger);
  font-size: var(--chanho-font-size-100);
}
```

- [ ] **Step 5: 컴포넌트 구현**

`design-system/packages/react/src/TextField/TextField.tsx`:

```tsx
import { useId } from "react";
import type { ComponentPropsWithRef } from "react";
import styles from "./TextField.module.css";

export interface TextFieldProps extends ComponentPropsWithRef<"input"> {
  /** 라벨 텍스트. 접근성을 위해 필수이며 input과 자동 연결된다. */
  label: string;
  /** 입력 아래 표시되는 보조 설명. */
  description?: string;
  /** 에러 메시지. 지정하면 aria-invalid와 에러 스타일이 적용된다. */
  error?: string;
}

/**
 * 한 줄 텍스트 입력. ref는 내부 input으로, className은 루트 래퍼로 전달된다.
 */
export function TextField({ label, description, error, id, className, ...rest }: TextFieldProps) {
  const autoId = useId();
  const inputId = id ?? autoId;
  const descId = `${inputId}-desc`;
  const errorId = `${inputId}-error`;
  const describedBy =
    [description ? descId : null, error ? errorId : null].filter(Boolean).join(" ") || undefined;

  return (
    <div className={[styles.field, className].filter(Boolean).join(" ")}>
      <label className={styles.label} htmlFor={inputId}>
        {label}
      </label>
      <input
        id={inputId}
        className={[styles.input, error ? styles.invalid : null].filter(Boolean).join(" ")}
        aria-invalid={error ? true : undefined}
        aria-describedby={describedBy}
        {...rest}
      />
      {description ? (
        <p id={descId} className={styles.description}>
          {description}
        </p>
      ) : null}
      {error ? (
        <p id={errorId} className={styles.error}>
          {error}
        </p>
      ) : null}
    </div>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(Spinner 뒤)에 추가:

```ts
export { TextField } from "./TextField/TextField";
export type { TextFieldProps } from "./TextField/TextField";
```

- [ ] **Step 6: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 27/27 PASS (기존 21 + TextField 6). 실출력 기록.

- [ ] **Step 7: 스토리 작성**

`design-system/packages/react/src/TextField/TextField.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { TextField } from "./TextField";

const meta = {
  title: "Components/TextField",
  component: TextField,
  args: { label: "이메일" },
} satisfies Meta<typeof TextField>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};

export const WithDescription: Story = {
  args: { description: "회사 이메일을 입력하세요" },
};

export const WithError: Story = {
  args: { error: "필수 항목입니다" },
};

export const Disabled: Story = { args: { disabled: true } };
```

- [ ] **Step 8: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/tokens/src/semantic.ts design-system/packages/react/src/TextField design-system/packages/react/src/index.ts
git commit -m "feat(react): TextField 컴포넌트 - 라벨/설명/에러 접근성 배선"
```

---

### Task 3: Checkbox 컴포넌트 (TDD, Radix)

**Files:**
- Create: `design-system/packages/react/src/Checkbox/Checkbox.test.tsx`
- Create: `design-system/packages/react/src/Checkbox/Checkbox.module.css`
- Create: `design-system/packages/react/src/Checkbox/Checkbox.tsx`
- Create: `design-system/packages/react/src/Checkbox/Checkbox.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: Task 1의 radix-ui (`Checkbox` 네임스페이스: Root/Indicator)
- Produces: `Checkbox`, `CheckboxProps` — `label: string`(필수) + Radix Checkbox.Root props(checked/defaultChecked/onCheckedChange/disabled 등, ref 포함)

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Checkbox/Checkbox.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { createRef } from "react";
import { describe, expect, it, vi } from "vitest";
import { Checkbox } from "./Checkbox";

describe("Checkbox", () => {
  it("라벨을 접근 가능 이름으로 가진 체크박스로 렌더링된다", () => {
    render(<Checkbox label="동의합니다" />);
    expect(screen.getByRole("checkbox", { name: "동의합니다" })).toBeInTheDocument();
  });

  it("클릭하면 체크 상태가 토글되고 onCheckedChange가 호출된다", async () => {
    const onCheckedChange = vi.fn();
    render(<Checkbox label="동의합니다" onCheckedChange={onCheckedChange} />);
    const box = screen.getByRole("checkbox", { name: "동의합니다" });
    await userEvent.click(box);
    expect(box).toBeChecked();
    expect(onCheckedChange).toHaveBeenCalledWith(true);
  });

  it("라벨을 클릭해도 토글된다", async () => {
    render(<Checkbox label="동의합니다" />);
    await userEvent.click(screen.getByText("동의합니다"));
    expect(screen.getByRole("checkbox", { name: "동의합니다" })).toBeChecked();
  });

  it("disabled면 클릭해도 토글되지 않는다", async () => {
    render(<Checkbox label="동의합니다" disabled />);
    const box = screen.getByRole("checkbox", { name: "동의합니다" });
    await userEvent.click(box);
    expect(box).not.toBeChecked();
  });

  it("소비자 className이 루트 래퍼에 병합된다", () => {
    const { container } = render(<Checkbox label="동의합니다" className="custom" />);
    expect(container.firstChild).toHaveClass("custom");
  });

  it("ref로 체크박스 버튼 DOM 노드에 접근할 수 있다", () => {
    const ref = createRef<HTMLButtonElement>();
    render(<Checkbox label="동의합니다" ref={ref} />);
    expect(ref.current).toBeInstanceOf(HTMLButtonElement);
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Checkbox` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Checkbox/Checkbox.module.css`:

```css
.wrapper {
  display: inline-flex;
  align-items: center;
  gap: var(--chanho-space-100);
  font-family: var(--chanho-font-family-sans);
}

.box {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
  padding: 0;
  border: 2px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-small);
  background: var(--chanho-color-background-surface);
  color: var(--chanho-color-text-inverse);
  cursor: pointer;
  transition: background-color 120ms ease, border-color 120ms ease;
}

.box:focus-visible {
  outline: 2px solid var(--chanho-color-border-focused);
  outline-offset: 2px;
}

.box[data-state="checked"] {
  border-color: var(--chanho-color-background-brand);
  background: var(--chanho-color-background-brand);
}

.box:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.indicator {
  display: inline-flex;
}

.label {
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  cursor: pointer;
}
```

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Checkbox/Checkbox.tsx`:

```tsx
import { useId } from "react";
import type { ComponentPropsWithRef } from "react";
import { Checkbox as RadixCheckbox } from "radix-ui";
import styles from "./Checkbox.module.css";

export interface CheckboxProps extends ComponentPropsWithRef<typeof RadixCheckbox.Root> {
  /** 라벨 텍스트. 접근성을 위해 필수이며 체크박스와 자동 연결된다. */
  label: string;
}

function CheckIcon() {
  return (
    <svg viewBox="0 0 12 12" width="12" height="12" fill="none" aria-hidden="true">
      <path
        d="M2.5 6.5L5 9l4.5-5.5"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}

export function Checkbox({ label, id, className, ...rest }: CheckboxProps) {
  const autoId = useId();
  const checkboxId = id ?? autoId;
  return (
    <span className={[styles.wrapper, className].filter(Boolean).join(" ")}>
      <RadixCheckbox.Root id={checkboxId} className={styles.box} {...rest}>
        <RadixCheckbox.Indicator className={styles.indicator}>
          <CheckIcon />
        </RadixCheckbox.Indicator>
      </RadixCheckbox.Root>
      <label className={styles.label} htmlFor={checkboxId}>
        {label}
      </label>
    </span>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(Button 뒤)에 추가:

```ts
export { Checkbox } from "./Checkbox/Checkbox";
export type { CheckboxProps } from "./Checkbox/Checkbox";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 33/33 PASS (27 + Checkbox 6). 실출력 기록.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Checkbox/Checkbox.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Checkbox } from "./Checkbox";

const meta = {
  title: "Components/Checkbox",
  component: Checkbox,
  args: { label: "동의합니다" },
} satisfies Meta<typeof Checkbox>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};

export const Checked: Story = { args: { defaultChecked: true } };

export const Disabled: Story = { args: { disabled: true } };

export const DisabledChecked: Story = { args: { disabled: true, defaultChecked: true } };
```

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Checkbox design-system/packages/react/src/index.ts
git commit -m "feat(react): Checkbox 컴포넌트 - Radix 기반, 라벨 자동 연결"
```

---

### Task 4: Radio / RadioGroup 컴포넌트 (TDD, Radix)

**Files:**
- Create: `design-system/packages/react/src/Radio/Radio.test.tsx`
- Create: `design-system/packages/react/src/Radio/Radio.module.css`
- Create: `design-system/packages/react/src/Radio/Radio.tsx`
- Create: `design-system/packages/react/src/Radio/Radio.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `RadioGroup` 네임스페이스 (Root/Item/Indicator)
- Produces: `RadioGroup`(Root 래퍼, value/defaultValue/onValueChange), `Radio`(Item + 라벨), `RadioGroupProps`, `RadioProps`. 화살표 키 이동은 Radix가 처리.

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Radio/Radio.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { createRef } from "react";
import { describe, expect, it } from "vitest";
import { Radio, RadioGroup } from "./Radio";

function Priorities(props: { defaultValue?: string }) {
  return (
    <RadioGroup aria-label="우선순위" defaultValue={props.defaultValue}>
      <Radio value="high" label="높음" />
      <Radio value="medium" label="보통" />
      <Radio value="low" label="낮음" />
    </RadioGroup>
  );
}

describe("Radio / RadioGroup", () => {
  it("radiogroup과 라벨 붙은 radio들로 렌더링된다", () => {
    render(<Priorities />);
    expect(screen.getByRole("radiogroup", { name: "우선순위" })).toBeInTheDocument();
    expect(screen.getAllByRole("radio")).toHaveLength(3);
    expect(screen.getByRole("radio", { name: "높음" })).toBeInTheDocument();
  });

  it("defaultValue에 해당하는 radio가 선택된 상태다", () => {
    render(<Priorities defaultValue="medium" />);
    expect(screen.getByRole("radio", { name: "보통" })).toBeChecked();
  });

  it("클릭하면 해당 radio가 선택된다", async () => {
    render(<Priorities />);
    await userEvent.click(screen.getByRole("radio", { name: "낮음" }));
    expect(screen.getByRole("radio", { name: "낮음" })).toBeChecked();
  });

  it("화살표 키로 선택을 이동할 수 있다", async () => {
    render(<Priorities defaultValue="high" />);
    await userEvent.click(screen.getByRole("radio", { name: "높음" }));
    await userEvent.keyboard("{ArrowDown}");
    expect(screen.getByRole("radio", { name: "보통" })).toBeChecked();
  });

  it("소비자 className이 그룹 루트에 병합된다", () => {
    const { container } = render(
      <RadioGroup aria-label="우선순위" className="custom">
        <Radio value="high" label="높음" />
      </RadioGroup>,
    );
    expect(container.firstChild).toHaveClass("custom");
  });

  it("ref로 radio 버튼 DOM 노드에 접근할 수 있다", () => {
    const ref = createRef<HTMLButtonElement>();
    render(
      <RadioGroup aria-label="우선순위">
        <Radio value="high" label="높음" ref={ref} />
      </RadioGroup>,
    );
    expect(ref.current).toBeInstanceOf(HTMLButtonElement);
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Radio` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Radio/Radio.module.css`:

```css
.group {
  display: flex;
  flex-direction: column;
  gap: var(--chanho-space-100);
  font-family: var(--chanho-font-family-sans);
}

.item {
  display: inline-flex;
  align-items: center;
  gap: var(--chanho-space-100);
}

.circle {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
  padding: 0;
  border: 2px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-full);
  background: var(--chanho-color-background-surface);
  cursor: pointer;
  transition: border-color 120ms ease;
}

.circle:focus-visible {
  outline: 2px solid var(--chanho-color-border-focused);
  outline-offset: 2px;
}

.circle[data-state="checked"] {
  border-color: var(--chanho-color-background-brand);
}

.circle:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.indicator {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 8px;
  height: 8px;
  border-radius: var(--chanho-radius-full);
  background: var(--chanho-color-background-brand);
}

.label {
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  cursor: pointer;
}
```

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Radio/Radio.tsx`:

```tsx
import { useId } from "react";
import type { ComponentPropsWithRef } from "react";
import { RadioGroup as RadixRadioGroup } from "radix-ui";
import styles from "./Radio.module.css";

export type RadioGroupProps = ComponentPropsWithRef<typeof RadixRadioGroup.Root>;

/** 라디오 버튼 묶음. 화살표 키 이동·단일 선택은 Radix가 처리한다. */
export function RadioGroup({ className, ...rest }: RadioGroupProps) {
  return (
    <RadixRadioGroup.Root
      className={[styles.group, className].filter(Boolean).join(" ")}
      {...rest}
    />
  );
}

export interface RadioProps extends ComponentPropsWithRef<typeof RadixRadioGroup.Item> {
  /** 라벨 텍스트. 접근성을 위해 필수이며 radio와 자동 연결된다. */
  label: string;
}

export function Radio({ label, id, className, ...rest }: RadioProps) {
  const autoId = useId();
  const radioId = id ?? autoId;
  return (
    <span className={[styles.item, className].filter(Boolean).join(" ")}>
      <RadixRadioGroup.Item id={radioId} className={styles.circle} {...rest}>
        <RadixRadioGroup.Indicator className={styles.indicator} />
      </RadixRadioGroup.Item>
      <label className={styles.label} htmlFor={radioId}>
        {label}
      </label>
    </span>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(Lozenge 뒤, Spinner 앞)에 추가:

```ts
export { Radio, RadioGroup } from "./Radio/Radio";
export type { RadioProps, RadioGroupProps } from "./Radio/Radio";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 39/39 PASS (33 + Radio 6). 실출력 기록.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Radio/Radio.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Radio, RadioGroup } from "./Radio";

const meta = {
  title: "Components/Radio",
  component: RadioGroup,
} satisfies Meta<typeof RadioGroup>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  render: () => (
    <RadioGroup aria-label="우선순위" defaultValue="medium">
      <Radio value="high" label="높음" />
      <Radio value="medium" label="보통" />
      <Radio value="low" label="낮음" />
    </RadioGroup>
  ),
};

export const WithDisabledItem: Story = {
  render: () => (
    <RadioGroup aria-label="우선순위" defaultValue="high">
      <Radio value="high" label="높음" />
      <Radio value="medium" label="보통" disabled />
      <Radio value="low" label="낮음" />
    </RadioGroup>
  ),
};
```

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Radio design-system/packages/react/src/index.ts
git commit -m "feat(react): Radio/RadioGroup 컴포넌트 - Radix 기반, 화살표 키 내비게이션"
```

---

### Task 5: Switch 컴포넌트 (TDD, Radix)

**Files:**
- Create: `design-system/packages/react/src/Switch/Switch.test.tsx`
- Create: `design-system/packages/react/src/Switch/Switch.module.css`
- Create: `design-system/packages/react/src/Switch/Switch.tsx`
- Create: `design-system/packages/react/src/Switch/Switch.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `Switch` 네임스페이스 (Root/Thumb)
- Produces: `Switch`, `SwitchProps` — `label: string`(필수) + Radix Switch.Root props (checked/defaultChecked/onCheckedChange/disabled, ref)

- [ ] **Step 1: 실패 테스트 작성**

`design-system/packages/react/src/Switch/Switch.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { createRef } from "react";
import { describe, expect, it, vi } from "vitest";
import { Switch } from "./Switch";

describe("Switch", () => {
  it("라벨을 접근 가능 이름으로 가진 switch로 렌더링된다", () => {
    render(<Switch label="알림 받기" />);
    expect(screen.getByRole("switch", { name: "알림 받기" })).toBeInTheDocument();
  });

  it("클릭하면 켜지고 onCheckedChange가 호출된다", async () => {
    const onCheckedChange = vi.fn();
    render(<Switch label="알림 받기" onCheckedChange={onCheckedChange} />);
    const sw = screen.getByRole("switch", { name: "알림 받기" });
    await userEvent.click(sw);
    expect(sw).toBeChecked();
    expect(onCheckedChange).toHaveBeenCalledWith(true);
  });

  it("disabled면 클릭해도 켜지지 않는다", async () => {
    render(<Switch label="알림 받기" disabled />);
    const sw = screen.getByRole("switch", { name: "알림 받기" });
    await userEvent.click(sw);
    expect(sw).not.toBeChecked();
  });

  it("소비자 className이 루트 래퍼에 병합된다", () => {
    const { container } = render(<Switch label="알림 받기" className="custom" />);
    expect(container.firstChild).toHaveClass("custom");
  });

  it("ref로 switch 버튼 DOM 노드에 접근할 수 있다", () => {
    const ref = createRef<HTMLButtonElement>();
    render(<Switch label="알림 받기" ref={ref} />);
    expect(ref.current).toBeInstanceOf(HTMLButtonElement);
  });
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Switch` 모듈 없음. 실출력 기록.

- [ ] **Step 3: 스타일 작성**

`design-system/packages/react/src/Switch/Switch.module.css`:

```css
.wrapper {
  display: inline-flex;
  align-items: center;
  gap: var(--chanho-space-100);
  font-family: var(--chanho-font-family-sans);
}

.track {
  position: relative;
  width: 32px;
  height: 18px;
  padding: 0;
  border: none;
  border-radius: var(--chanho-radius-full);
  background: var(--chanho-color-background-neutral-hovered);
  cursor: pointer;
  transition: background-color 120ms ease;
}

.track:focus-visible {
  outline: 2px solid var(--chanho-color-border-focused);
  outline-offset: 2px;
}

.track[data-state="checked"] {
  background: var(--chanho-color-background-brand);
}

.track:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.thumb {
  display: block;
  width: 14px;
  height: 14px;
  border-radius: var(--chanho-radius-full);
  background: var(--chanho-color-background-surface);
  transform: translateX(2px);
  transition: transform 120ms ease;
  will-change: transform;
}

.thumb[data-state="checked"] {
  transform: translateX(16px);
}

.label {
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  cursor: pointer;
}
```

- [ ] **Step 4: 컴포넌트 구현**

`design-system/packages/react/src/Switch/Switch.tsx`:

```tsx
import { useId } from "react";
import type { ComponentPropsWithRef } from "react";
import { Switch as RadixSwitch } from "radix-ui";
import styles from "./Switch.module.css";

export interface SwitchProps extends ComponentPropsWithRef<typeof RadixSwitch.Root> {
  /** 라벨 텍스트. 접근성을 위해 필수이며 switch와 자동 연결된다. */
  label: string;
}

export function Switch({ label, id, className, ...rest }: SwitchProps) {
  const autoId = useId();
  const switchId = id ?? autoId;
  return (
    <span className={[styles.wrapper, className].filter(Boolean).join(" ")}>
      <RadixSwitch.Root id={switchId} className={styles.track} {...rest}>
        <RadixSwitch.Thumb className={styles.thumb} />
      </RadixSwitch.Root>
      <label className={styles.label} htmlFor={switchId}>
        {label}
      </label>
    </span>
  );
}
```

`design-system/packages/react/src/index.ts` — 알파벳 순서(Spinner 뒤, TextField 앞)에 추가:

```ts
export { Switch } from "./Switch/Switch";
export type { SwitchProps } from "./Switch/Switch";
```

- [ ] **Step 5: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 44/44 PASS (39 + Switch 5). 실출력 기록.

- [ ] **Step 6: 스토리 작성**

`design-system/packages/react/src/Switch/Switch.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Switch } from "./Switch";

const meta = {
  title: "Components/Switch",
  component: Switch,
  args: { label: "알림 받기" },
} satisfies Meta<typeof Switch>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};

export const On: Story = { args: { defaultChecked: true } };

export const Disabled: Story = { args: { disabled: true } };
```

- [ ] **Step 7: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/src/Switch design-system/packages/react/src/index.ts
git commit -m "feat(react): Switch 컴포넌트 - Radix 기반 토글"
```

---

### Task 6: Select 컴포넌트 (TDD, Radix) + Wave 2 최종 통합 검증

**Files:**
- Modify: `design-system/packages/react/vitest.setup.ts` (jsdom 폴리필 — Radix Select 필요)
- Create: `design-system/packages/react/src/Select/Select.test.tsx`
- Create: `design-system/packages/react/src/Select/Select.module.css`
- Create: `design-system/packages/react/src/Select/Select.tsx`
- Create: `design-system/packages/react/src/Select/Select.stories.tsx`
- Modify: `design-system/packages/react/src/index.ts`

**Interfaces:**
- Consumes: radix-ui `Select` 네임스페이스 (Root/Trigger/Value/Icon/Portal/Content/Viewport/Item/ItemText/ItemIndicator)
- Produces: `Select`, `SelectProps`, `SelectOption` — 평탄한 options 배열 API(그룹은 YAGNI, B3+에서 필요 시): `label`(필수), `options: {value,label}[]`, `value?/defaultValue?/onValueChange?/placeholder?/disabled?/className?/id?`. 합성 루트라 ref 미노출(테스트 파일 주석으로 명시).

- [ ] **Step 1: jsdom 폴리필 추가**

`design-system/packages/react/vitest.setup.ts` 전체를 다음으로 교체:

```ts
import "@testing-library/jest-dom/vitest";
import { afterEach } from "vitest";
import { cleanup } from "@testing-library/react";

afterEach(() => {
  cleanup();
});

// Radix Select가 사용하는 API 중 jsdom에 없는 것들의 폴리필
class ResizeObserverStub {
  observe() {}
  unobserve() {}
  disconnect() {}
}
if (!("ResizeObserver" in globalThis)) {
  (globalThis as unknown as { ResizeObserver: typeof ResizeObserverStub }).ResizeObserver =
    ResizeObserverStub;
}
if (!window.HTMLElement.prototype.scrollIntoView) {
  window.HTMLElement.prototype.scrollIntoView = () => {};
}
if (!window.HTMLElement.prototype.hasPointerCapture) {
  window.HTMLElement.prototype.hasPointerCapture = () => false;
}
if (!window.HTMLElement.prototype.releasePointerCapture) {
  window.HTMLElement.prototype.releasePointerCapture = () => {};
}
```

주의: 기존 파일에 afterEach cleanup이 이미 있으므로(B1) 위 내용은 그것을 보존한 채 폴리필만 추가하는 형태다. 기존 내용과 다르면 기존 cleanup 로직을 유지하면서 폴리필 블록만 추가한다.

Run: `pnpm --filter @chanho/react test`
Expected: 44/44 PASS (폴리필이 기존 테스트를 깨지 않음)

- [ ] **Step 2: 실패 테스트 작성**

`design-system/packages/react/src/Select/Select.test.tsx`:

```tsx
// Select는 Radix 합성 컴포넌트를 감싼 복합 루트라 ref를 노출하지 않는다
// (공통 테스트 3종 중 ref 테스트 예외 — Global Constraints 참조).
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { Select } from "./Select";

const OPTIONS = [
  { value: "high", label: "높음" },
  { value: "medium", label: "보통" },
  { value: "low", label: "낮음" },
];

describe("Select", () => {
  it("라벨을 접근 가능 이름으로 가진 combobox로 렌더링된다", () => {
    render(<Select label="우선순위" options={OPTIONS} placeholder="선택" />);
    expect(screen.getByRole("combobox", { name: "우선순위" })).toBeInTheDocument();
  });

  it("트리거를 클릭하면 옵션 목록이 열린다", async () => {
    render(<Select label="우선순위" options={OPTIONS} placeholder="선택" />);
    await userEvent.click(screen.getByRole("combobox", { name: "우선순위" }));
    expect(screen.getByRole("option", { name: "높음" })).toBeInTheDocument();
    expect(screen.getAllByRole("option")).toHaveLength(3);
  });

  it("옵션을 선택하면 onValueChange가 호출되고 트리거에 라벨이 표시된다", async () => {
    const onValueChange = vi.fn();
    render(
      <Select label="우선순위" options={OPTIONS} placeholder="선택" onValueChange={onValueChange} />,
    );
    const trigger = screen.getByRole("combobox", { name: "우선순위" });
    await userEvent.click(trigger);
    await userEvent.click(screen.getByRole("option", { name: "보통" }));
    expect(onValueChange).toHaveBeenCalledWith("medium");
    expect(trigger).toHaveTextContent("보통");
  });

  it("defaultValue가 트리거에 표시된다", () => {
    render(<Select label="우선순위" options={OPTIONS} defaultValue="low" />);
    expect(screen.getByRole("combobox", { name: "우선순위" })).toHaveTextContent("낮음");
  });

  it("disabled면 트리거가 비활성화된다", () => {
    render(<Select label="우선순위" options={OPTIONS} disabled />);
    expect(screen.getByRole("combobox", { name: "우선순위" })).toBeDisabled();
  });

  it("소비자 className이 루트 래퍼에 병합된다", () => {
    const { container } = render(
      <Select label="우선순위" options={OPTIONS} className="custom" />,
    );
    expect(container.firstChild).toHaveClass("custom");
  });
});
```

- [ ] **Step 3: RED 확인**

Run: `pnpm --filter @chanho/react test`
Expected: FAIL — `./Select` 모듈 없음. 실출력 기록.

- [ ] **Step 4: 스타일 작성**

`design-system/packages/react/src/Select/Select.module.css`:

```css
.field {
  display: inline-flex;
  flex-direction: column;
  gap: var(--chanho-space-50);
  font-family: var(--chanho-font-family-sans);
}

.label {
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  font-weight: var(--chanho-font-weight-medium);
}

.trigger {
  display: inline-flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--chanho-space-100);
  min-width: 180px;
  height: 40px;
  padding: 0 var(--chanho-space-100);
  border: 2px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-medium);
  background: var(--chanho-color-background-surface);
  color: var(--chanho-color-text-default);
  font-family: var(--chanho-font-family-sans);
  font-size: var(--chanho-font-size-200);
  cursor: pointer;
  transition: border-color 120ms ease;
}

.trigger:hover:not(:disabled) {
  background: var(--chanho-color-background-subtle);
}

.trigger:focus-visible {
  outline: none;
  border-color: var(--chanho-color-border-focused);
}

.trigger:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.trigger[data-placeholder] {
  color: var(--chanho-color-text-subtle);
}

.content {
  overflow: hidden;
  min-width: 180px;
  border: 1px solid var(--chanho-color-border-default);
  border-radius: var(--chanho-radius-medium);
  background: var(--chanho-color-background-surface);
  box-shadow: 0 4px 12px var(--chanho-color-background-neutral-hovered);
  font-family: var(--chanho-font-family-sans);
}

.item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--chanho-space-100);
  padding: var(--chanho-space-100) var(--chanho-space-150);
  color: var(--chanho-color-text-default);
  font-size: var(--chanho-font-size-200);
  cursor: pointer;
  user-select: none;
  outline: none;
}

.item[data-highlighted] {
  background: var(--chanho-color-background-brand-subtle);
  color: var(--chanho-color-text-brand);
}

.item[data-disabled] {
  cursor: not-allowed;
  opacity: 0.5;
}
```

- [ ] **Step 5: 컴포넌트 구현**

`design-system/packages/react/src/Select/Select.tsx`:

```tsx
import { useId } from "react";
import { Select as RadixSelect } from "radix-ui";
import styles from "./Select.module.css";

export interface SelectOption {
  value: string;
  label: string;
}

export interface SelectProps {
  /** 라벨 텍스트. 접근성을 위해 필수이며 트리거와 자동 연결된다. */
  label: string;
  /** 선택지 목록. */
  options: SelectOption[];
  /** 제어 값. */
  value?: string;
  /** 비제어 초기 값. */
  defaultValue?: string;
  /** 값 변경 콜백. */
  onValueChange?: (value: string) => void;
  /** 미선택 시 표시 문구. */
  placeholder?: string;
  disabled?: boolean;
  /** 루트 래퍼에 병합되는 클래스. */
  className?: string;
  id?: string;
}

function ChevronIcon() {
  return (
    <svg viewBox="0 0 12 12" width="12" height="12" fill="none" aria-hidden="true">
      <path
        d="M2.5 4.5L6 8l3.5-3.5"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}

export function Select({
  label,
  options,
  value,
  defaultValue,
  onValueChange,
  placeholder,
  disabled,
  className,
  id,
}: SelectProps) {
  const autoId = useId();
  const triggerId = id ?? autoId;
  return (
    <div className={[styles.field, className].filter(Boolean).join(" ")}>
      <label className={styles.label} htmlFor={triggerId}>
        {label}
      </label>
      <RadixSelect.Root
        value={value}
        defaultValue={defaultValue}
        onValueChange={onValueChange}
        disabled={disabled}
      >
        <RadixSelect.Trigger id={triggerId} className={styles.trigger}>
          <RadixSelect.Value placeholder={placeholder} />
          <RadixSelect.Icon>
            <ChevronIcon />
          </RadixSelect.Icon>
        </RadixSelect.Trigger>
        <RadixSelect.Portal>
          <RadixSelect.Content className={styles.content} position="popper" sideOffset={4}>
            <RadixSelect.Viewport>
              {options.map((option) => (
                <RadixSelect.Item key={option.value} value={option.value} className={styles.item}>
                  <RadixSelect.ItemText>{option.label}</RadixSelect.ItemText>
                  <RadixSelect.ItemIndicator>
                    <ChevronIcon />
                  </RadixSelect.ItemIndicator>
                </RadixSelect.Item>
              ))}
            </RadixSelect.Viewport>
          </RadixSelect.Content>
        </RadixSelect.Portal>
      </RadixSelect.Root>
    </div>
  );
}
```

주의: ItemIndicator의 체크 표시로 ChevronIcon을 재사용하지 말고 싶다면 별도 아이콘을 만들 수 있으나, 아이콘 세트는 Phase 4 범위이므로 이번에는 재사용한다(구현 그대로).

`design-system/packages/react/src/index.ts` — 알파벳 순서(Radio 뒤, Spinner 앞)에 추가:

```ts
export { Select } from "./Select/Select";
export type { SelectOption, SelectProps } from "./Select/Select";
```

- [ ] **Step 6: GREEN 확인**

Run: `pnpm --filter @chanho/react test`
Expected: 50/50 PASS (44 + Select 6). 실출력 기록. Radix Select의 jsdom 상호작용이 폴리필로도 실패하면(열림/클릭 문제) 증상과 시도를 리포트에 남기고 BLOCKED로 에스컬레이션 — 테스트를 약화(구조 검증화)하지 말 것.

- [ ] **Step 7: 스토리 작성**

`design-system/packages/react/src/Select/Select.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite";
import { Select } from "./Select";

const OPTIONS = [
  { value: "high", label: "높음" },
  { value: "medium", label: "보통" },
  { value: "low", label: "낮음" },
];

const meta = {
  title: "Components/Select",
  component: Select,
  args: { label: "우선순위", options: OPTIONS, placeholder: "선택하세요" },
} satisfies Meta<typeof Select>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};

export const Preselected: Story = { args: { defaultValue: "medium" } };

export const Disabled: Story = { args: { disabled: true } };
```

- [ ] **Step 8: Wave 2 최종 통합 검증**

Run: `cd C:/MSA_TEMPLATE/design-system && pnpm typecheck && pnpm test && pnpm lint:css && pnpm build-storybook`
Expected: typecheck 3패키지 / 54/54 (tokens 4 + react 50) / lint 통과 / Storybook 빌드 성공 — storybook-static/index.json에 TextField/Checkbox/Radio/Switch/Select 스토리 포함 확인

- [ ] **Step 9: Commit**

```bash
cd C:/MSA_TEMPLATE
git add design-system/packages/react/vitest.setup.ts design-system/packages/react/src/Select design-system/packages/react/src/index.ts
git commit -m "feat(react): Select 컴포넌트 - Radix 기반, jsdom 폴리필 포함"
```

---

## 완료 기준 (Plan B2 전체)

- `pnpm typecheck` — 3패키지 에러 0
- `pnpm test` — 54/54 (tokens 4 + react 50: 기존 21 + TextField 6 + Checkbox 6 + Radio 6 + Switch 5 + Select 6)
- `pnpm lint:css` — 통과
- `pnpm build-storybook` — 성공, Components에 10개 컴포넌트 스토리
- lockfile에 vite 6 트리 없음 (메이저 단일화)
- 수동(브라우저): Select 팝오버가 다크모드에서 올바른 표면색으로 뜨는지, 폼 컨트롤 키보드 조작(Tab/Space/화살표), a11y 애드온 위반 0건
- 이후 **Plan B3**(Modal/Dropdown/Tooltip/Toast + Tabs) 작성
